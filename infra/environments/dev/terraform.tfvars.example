# Copy this file to terraform.tfvars (ignored by git) and adjust the values
# before running terraform plan/apply locally.

account_id              = "123456789012"
grafana_admin_password  = "change-me" # Prefer secrets manager or Vault
database_password        = "postgres-password"
mongo_password           = "mongo-password"

